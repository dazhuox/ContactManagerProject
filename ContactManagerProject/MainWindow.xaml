<Window x:Class="ContactManagerProject.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ContactManagerProject"
        mc:Ignorable="d"
        Title="MainWindow" Height="844" Width="390">

    <Grid Background="LightBlue">

        <!--First Row-->
        <TextBox x:Name="Title" HorizontalAlignment="Right" Margin="0,10,154,0" TextWrapping="Wrap" Text="Contacts" VerticalAlignment="Top" Width="230" Height="75"  FontSize="50" Background="AliceBlue"/>
        <Button x:Name="AddContact" Content="+" Margin="305,10,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Height="75" Width="75" Click="AddContactDoubleClick" FontSize="30" Background="LightBlue" BorderThickness="0.5" Foreground="AliceBlue"/>
        <!--Second Row-->


        <!-- Image!  Image! Image! Image! Image! Image!
        
        <Ellipse x:Name="OwnPfp" HorizontalAlignment="Left" Margin="10,104,0,0" Stroke="Black" VerticalAlignment="Top" Width="75" Height="75">
            <Ellipse.Fill>
                <ImageBrush ImageSource="image.png"/>
            </Ellipse.Fill>
        </Ellipse> 
        -->

        <TextBox x:Name="OwnProfile" HorizontalAlignment="Left" Margin="98,107,0,0" TextWrapping="Wrap" Text="My profile" VerticalAlignment="Top" Width="266" Height="69"  FontSize="40"/>

        <DataGrid x:Name="ContactList" HorizontalAlignment="Center" Margin="0,284,0,0" VerticalAlignment="Top" Width="348" Height="394" AutoGenerateColumns="False" MouseDoubleClick="ContactList_MouseDoubleClick" SelectionChanged="ContactList_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Salutation}" Header="Salutation"/>
                <DataGridTextColumn Binding="{Binding FirstName}" Header="First Name"/>
                <DataGridTextColumn Binding="{Binding MiddleName}" Header="Middle Name"/>
                <DataGridTextColumn Binding="{Binding LastName}" Header="Last Name"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBox x:Name="ContactsCount" HorizontalAlignment="Center" Margin="0,769,0,0" TextWrapping="Wrap" Text="x number of Contacts" VerticalAlignment="Top" Width="390" Height="59" RenderTransformOrigin="0.5,0.5" FontSize="15"/>

    </Grid>
</Window>
